# Public npm registry for @pixel-loader scope
@pixel-loader:registry=https://registry.npmjs.org/

# Always publish with public access
access=public

# Use engine-strict for package.json engines check
engine-strict=true
